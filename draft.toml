[environments]
  [environments.development]
    name = "requestbin"
    namespace = "requestbin"
    wait = true
    watch = false
    watch-delay = 2
    auto-connect = false
    dockerfile = "Dockerfile"
    chart = ""

